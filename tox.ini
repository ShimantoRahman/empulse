[tox]
minversion = 3.8.0
envlist = py3{11,12,13,14}-{tests,docs,lint}, py314t-tests
isolated_build = true

[gh-actions]
python =
    3.11: py311-{tests, docs, lint}
    3.12: py312-{tests, docs, lint}
    3.13: py313-{tests, docs, lint}
    3.14: py314-{tests, docs, lint}
    3.14t: py314t-tests

[testenv]
package = wheel
;wheel_build_env = .pkg
setenv =
    PYTHONPATH = {toxinidir}

[testenv:py3{11,12,13,14}-lint]
description = run code formatter and linter (auto-fix)
skip_install = true
deps =
    pre-commit-uv>=4.1.1
commands =
    pre-commit run --all-files --show-diff-on-failure

[testenv:py3{11,12,13,14}-docs]
runner = uv-venv-lock-runner
description = build documentation with sphinx
dependency_groups =
    docs
commands =
    sphinx-build -b html --write-all --fail-on-warning --keep-going docs/ docs/_build

[testenv:py3{11,12,13,14}-tests]
runner = uv-venv-lock-runner
description = run tests with pytest
dependency_groups =
    test
commands =
    python setup.py build_ext --inplace
    pytest --basetemp={envtmpdir} {posargs}
    pytest --doctest-modules empulse/ --ignore=empulse/metrics/_loss --ignore=empulse/metrics/_cy_convex_hull --ignore=empulse/models/cost_sensitive/_impurity --ignore=empulse/models/cy_proftree

[testenv:py314t-tests]
description = run tests on the free-threaded interpreter (CPython 3.14t)
base_python = py314t
# Resolved fresh rather than from uv.lock, which pins two packages that publish no cp314t wheel:
# catboost and polars. xgboost and lightgbm ship py3-none wheels and do install here. The tests
# that need the missing two skip through pytest.importorskip; tests/datasets is skipped wholesale
# because its modules import polars at module scope. Drop these exclusions once wheels exist.
runner = uv-venv-runner
extras = symbolic
deps =
    pytest>=8.3.4
    pytest-cov>=6.0.0
    pandas>=2.2.3
    xgboost>=1.7.4
    lightgbm>=4.5.0
    cython==3.2.5
commands =
    python setup.py build_ext --inplace
    pytest --basetemp={envtmpdir} --ignore=tests/datasets {posargs}

# >>> sklearn-compat-start (auto-generated, do not edit manually)

[testenv:sklearn152-tests]
runner = uv-venv-runner
basepython = python3.12
description = run tests against scikit-learn 1.5.2
dependency_groups =
    test
deps =
    scikit-learn==1.5.2
commands =
    python setup.py build_ext --inplace
    pytest --basetemp={envtmpdir} -m "not slow" {posargs}

[testenv:sklearn161-tests]
runner = uv-venv-runner
basepython = python3.12
description = run tests against scikit-learn 1.6.1
dependency_groups =
    test
deps =
    scikit-learn==1.6.1
commands =
    python setup.py build_ext --inplace
    pytest --basetemp={envtmpdir} -m "not slow" {posargs}

[testenv:sklearn172-tests]
runner = uv-venv-runner
basepython = python3.12
description = run tests against scikit-learn 1.7.2
dependency_groups =
    test
deps =
    scikit-learn==1.7.2
commands =
    python setup.py build_ext --inplace
    pytest --basetemp={envtmpdir} -m "not slow" {posargs}

[testenv:sklearn180-tests]
runner = uv-venv-runner
basepython = python3.12
description = run tests against scikit-learn 1.8.0
dependency_groups =
    test
deps =
    scikit-learn==1.8.0
commands =
    python setup.py build_ext --inplace
    pytest --basetemp={envtmpdir} -m "not slow" {posargs}

# >>> sklearn-compat-end
